import Link from 'next/link'

export default function Download(){const url=process.env.NEXT_PUBLIC_ANDROID_DOWNLOAD_URL;return <main className="page"><div className="container"><nav className="nav"><Link href="/" className="brand"><span className="logo">▶</span>STREAM TOOLS</Link><Link href="/dashboard" className="btn">Dashboard</Link></nav><section className="hero"><span className="eyebrow">ANDROID</span><h1>Stream Tools<br/><span className="gradient">Mobile App</span></h1><p>Login di aplikasi menggunakan akun website yang sama. Status Premium akan mengikuti akun server.</p><div className="actions">{url?<a className="btn primary" href={url}>⬇ Download APK</a>:<span className="btn" style={{opacity:.7}}>APK belum dikonfigurasi</span>}</div><div className="card" style={{maxWidth:650,margin:'35px auto 0',textAlign:'left'}}><h3>Catatan rilis</h3><p className="muted">Link APK diatur melalui environment variable Vercel: <span className="codebox">NEXT_PUBLIC_ANDROID_DOWNLOAD_URL</span>.</p></div></section></div></main>}
