'use client'
import Link from 'next/link'
import { FormEvent,useState } from 'react'
import { createClient } from '../../../lib/supabase/client'

export default function SignUpPage(){const supabase=createClient();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);const {data,error}=await supabase.auth.signUp({email,password,options:{data:{display_name:name},emailRedirectTo:`${window.location.origin}/auth/callback`}});setLoading(false);if(error){setError(error.message);return}if(data.session) location.href='/dashboard';else setDone(true)}return <main className="auth"><div className="authbox"><Link href="/" className="brand"><img src="/images/logo.png" alt="Stream Tools"/></Link><div className="card"><span className="eyebrow">CREATE ACCOUNT</span><h1>Buat akun baru</h1><p className="sub">Satu akun untuk website dan aplikasi Stream Tools.</p>{error&&<div className="notice error">{error}</div>}{done?<div className="notice success">Akun berhasil dibuat. Cek email kamu untuk konfirmasi, lalu login.</div>:<form onSubmit={submit}><label className="label">Nama</label><input className="input" required value={name} onChange={e=>setName(e.target.value)} placeholder="Nama kamu"/><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="kamu@email.com"/><label className="label">Password</label><input className="input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimal 8 karakter"/><button className="btn primary full" disabled={loading}>{loading?'Membuat akun…':'Buat Akun'}</button></form>}<p className="muted small center" style={{marginTop:18}}>Sudah punya akun? <Link href="/auth/login" style={{color:'#c4b5fd'}}>Login</Link></p></div></div></main>}
